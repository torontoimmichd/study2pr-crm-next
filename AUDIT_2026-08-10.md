# Study2PR Production Audit - 2026-08-10

## Verdict

**FRAGILE.** The public application shell is reachable, the live database now exposes the sql/70 application lifecycle contracts, and deployed Edge Functions reject unauthenticated requests as expected. I cannot certify the CRM as safe for real clients because this session has no authenticated staff session or database password, so the required read-only checks of `information_schema`, `pg_get_functiondef`, `pg_policies`, `cron.job`, money reconciliation, lead-task canary, RLS coverage, and the twelve refusal guards could not be run. The repository and production also drift: production has seven Edge Functions while the repo has six local function directories, `wa-send` differs from the deployed source, sql/70 is not represented by a repository migration, and production `/ircc-tracker` is still 404.

## Severity-ordered Findings

### P1 - Production and repository are not reproducible

**Verified working in production:** Supabase currently reports these active Edge Functions:

| Function | Version | JWT |
|---|---:|---|
| `send-template-test` | 12 | required |
| `invite-staff` | 12 | required |
| `whatsapp-webhook` | 11 | disabled; provider HMAC is expected |
| `comms-worker` | 14 | disabled; worker secret is expected |
| `wa-send` | 13 | required |
| `outbox-send` | 1 | disabled; worker secret is expected |
| `ircc-fill` | 1 | required |

The repository has `ask-immigration-ai` and `process-ircc-email`, but neither is deployed. Production has `invite-staff`, `outbox-send`, and `ircc-fill`, but none has a matching local source directory. The deployed `wa-send` source also differs from the repository copy. A future deploy from this repo cannot reproduce the current production behavior.

### P1 - Production frontend is behind the current repository

Read-only HTTP checks:

| Route | Result |
|---|---|
| `/dashboard` | HTTP 200 |
| `/applications` | HTTP 200 |
| `/portal/login` | HTTP 200 |
| `/ircc-tracker` | HTTP 404; matched `/404` |

The repository contains the IRCC tracker route. This is deployment drift, not a route-code failure. Authenticated rendering and data loading were not checked.

### P1 - Full build is blocked by existing lint debt

- TypeScript: **0 errors**.
- Full build: compilation succeeds, then stops during linting.
- Existing ESLint errors: **74** (`45` `no-explicit-any`, `29` JSX entity errors).
- Existing warnings: **3**.
- The modified application lifecycle files pass focused ESLint.

### P1 - Required live safety checks were not executable from this session

No database password, service-role key, authenticated browser session, or direct Postgres connection was available. PostgREST correctly rejected direct catalog probes for `cron.job`, `information_schema.columns`, and `pg_policies`; these are not exposed as public API tables. The following are therefore **Not checked**, not assumed healthy:

- all eight cron jobs and the last-24-hour failure count;
- `pg_get_functiondef` for live trigger and RPC bodies;
- all 211 live RLS policies and the 139-table RLS-disabled claim;
- the twelve deliberate refusal guards from the operating description;
- the lead-task canary (`max open tasks per lead = 1`);
- the four money metrics and the `v_cash_not_deposited` result;
- live secure-link, portal, assessment, document, and payment end-to-end behavior;
- public signups, MFA, and database-password rotation.

## Live Schema Checks That Were Possible

### Application lifecycle: Verified

The live `case_stages_ref` endpoint returned:

`intake`, `documents`, `review`, `submission`, `ircc_processing`, `decision`, `approved`, `refused`, `withdrawn`, and `closed`.

`approved`, `refused`, `withdrawn`, and `closed` are terminal. `closed` is sort order 100.

The live-generated TypeScript schema contains:

- `v_completed_applications`;
- `v_open_outcome_reviews`;
- `v_outstanding_money`;
- `case_outcome_reviews` legal-acceptance fields;
- `cases.pending_stage_note`;
- `mark_case_outcome` with the sql/70 argument signature.

The completed and open-review views exist but anonymous access returned HTTP 401 because their security-invoker dependencies require staff access. `v_outstanding_money` returned HTTP 200 with no anonymous rows. Counts and contents were **Not checked** without authentication.

### Storage: Partially verified

The application source has no literal `case-documents` storage bucket. Static storage calls use `case-files`; the communications worker uses `comms-media`; document downloads also use the row's stored bucket dynamically. The anonymous storage bucket listing returned `[]`, so the live bucket inventory was **Not checked** in this session.

### Lead-task ownership: Repository verified, live canary not checked

The repository definitions of `fn_engine_on_lead_created` contain the ownership comment and no task insert. The active migration and baseline definitions queue messages only. The repo no longer contains the four-task lead creation block described in the older operating document.

The live function body and the maximum-open-task canary were **Not checked** because both require direct database access. This must be run before deploying any schema or function changes.

## Repository Application Audit

The current working tree contains the following local lifecycle work from the preceding implementation task, but it is not deployed by this audit:

- outcome actions use only `mark_case_outcome`; the direct-update fallback is absent;
- Withdrawn and Closed actions are exposed beside Approved and Refused;
- refusal reviews read `v_open_outcome_reviews` and record path, rationale, decision time, and judicial-review legal acceptance;
- Completed uses `v_completed_applications` and displays paid/outstanding amounts and archive state;
- Pending Payment uses `v_outstanding_money`, excludes terminal stages, and cross-checks `cases.is_archived = false`;
- active application queries exclude archived cases;
- Kanban stage changes prompt for a note and write `pending_stage_note`;
- live Supabase types were regenerated after sql/70.

Focused checks passed for the modified lifecycle files:

- `npx tsc --noEmit`
- focused ESLint on the modified files
- `git diff --check`

The full production build remains blocked by the pre-existing 74-error lint baseline. The repository working tree was already dirty before this audit; no application source was changed while performing this read-only audit. This report is the only file added by this audit.

## Edge Function Runtime Smoke Tests

Unauthenticated HTTP probes returned the expected boundary responses:

- `send-template-test`: 401
- `invite-staff`: 401
- `wa-send`: 401
- `ircc-fill`: 401
- `whatsapp-webhook`: 403
- `comms-worker`: 403
- `outbox-send`: 403

These checks verify the edge boundary only. They do not verify secrets, cron invocation, database writes, provider delivery, or successful authenticated execution.

## Status by Operating-Description Area

| Area | Status | Evidence / limitation |
|---|---|---|
| Lead creation one-task rule | Not checked live; repository verified | Live `pg_get_functiondef` and canary unavailable |
| Lead ladder and SLA sweeps | Not checked | Cron catalog and live function bodies unavailable |
| Assessment confirmation and scoring guards | Not checked | Requires rolled-back authenticated DB tests |
| Four application terminal outcomes | Partially verified | Live stage list verified; write guard tests not run |
| Refusal review and JR locks | Partially verified | Live schema/types verified; authenticated write/constraint tests not run |
| Completed/outstanding application views | Partially verified | Endpoints exist; authenticated rows/counts unavailable |
| Money controls and reconciliation | Not checked | No direct DB connection |
| RLS and data exposure | Not checked | Catalog/policy access unavailable; no authenticated data audit |
| Storage bucket inventory | Not checked | Anonymous bucket listing is intentionally empty |
| Edge Function endpoint boundaries | Verified | Seven deployed functions returned expected unauthenticated responses |
| Production route deployment | Fragile | `/ircc-tracker` is 404 while present in repo |
| Build/release gate | Fragile | TypeScript clean; full build blocked by 74 existing lint errors |

## Required Follow-up Before Calling This Verified Working

1. Run the SQL-only audit with an owner-controlled database connection, using transactions and rollback for the twelve refusal tests.
2. Compare every live `pg_get_functiondef` result with tracked source, especially the lead-created trigger, outcome RPC, payment guards, assessment guards, and cron sweep functions.
3. Export and reconcile `cron.job`, `cron.job_run_details`, `pg_policies`, `information_schema.columns`, storage buckets, the four money metrics, and the lead-task canary.
4. Synchronize deployed-only Edge Functions and the deployed `wa-send v13` source into the repository, or explicitly retire them.
5. Deploy the current frontend only after the authenticated route/data checks pass, then confirm `/ircc-tracker` is no longer 404.

