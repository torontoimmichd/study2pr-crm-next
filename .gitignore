# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# next.js
.next/
out/
next-env.d.ts

# Supabase CLI local machine state (project ref, pooler URL, server versions).
# Machine-specific, not source. No secrets in it - verified 1 Aug 2026.
supabase/.temp/

# Build/tool scratch
tsconfig.tsbuildinfo
work/

# This project is npm-based; package-lock.json is the committed lockfile.
# A stray pnpm-lock.yaml would make Vercel switch package managers and
# ignore it, re-opening the dependency drift Phase 2B closed.
pnpm-lock.yaml
yarn.lock
